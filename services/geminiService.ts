/**
 * DEPRECATED: Project is now running exclusively on local ML
 */
export const analyzePosts = async () => {
  console.warn("Gemini Service is disabled. Using local ML engine instead.");
  return null;
};
